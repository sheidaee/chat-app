import React from 'react';

import styles from './Toolbar.module.scss';
import NavigationItems from '../NavigationItems';

const Toolbar = () => {
  return <div className={styles.toolbar}>{/* <NavigationItems /> */}</div>;
};

export default Toolbar;
